name: mobile
version: 1.0.0
entry: ori/main.ori
platform: android

# A native Android app. `ori build mobile` cross-compiles the C VM with the NDK,
# packages an APK (the Ori program runs via JNI), then:
#   adb install -r mobile/build/mobile.apk
dependencies:
